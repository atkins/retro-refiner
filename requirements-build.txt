# Pinned build toolchain for the release workflow (.github/workflows/build.yml).
#
# These are pinned deliberately. Release builds were previously unpinned, so a
# toolchain upgrade between two releases silently changed the artifact: Nuitka
# 4.1.x ships a pywebview plugin whose allowlist of Windows platform modules
# predates pywebview 6.x adding webview/platforms/win32.py, and it excluded that
# module from the compile. The build stayed green and the GUI died on launch.
#
# Bump these intentionally, and confirm `--check-gui` still passes on the built
# artifact before releasing.

nuitka[onefile]==4.1.3
pywebview==6.2.1

beautifulsoup4==4.15.0
httpx==0.28.1
humanize==4.16.0
loguru==0.7.3
orjson==3.11.9
pyyaml==6.0.3
tenacity==9.1.4
truststore==0.10.4
