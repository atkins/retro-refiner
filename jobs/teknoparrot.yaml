# TeknoParrot — arcade collection test
# Note: destination is intentionally omitted (defaults to ./refined)

sources:
  - "https://myrient.erista.me/files/TeknoParrot/"

selection:
  english_only: true

output:
  local_file_action: move

advanced:
  cache_dir: "./cache"
  dat_dir: "./dat_files"
